# Design Notes

実装中に気づいたデザイン・UX・品質改善点のメモ。
**アプリを最後まで作り切ることを優先**し、ここに記録したものは Phase 1 完了後にまとめて対応する。

---

## 📋 未対応の改善案

### カード一覧の `note` 表示
- **日付**: 2026-04-24
- **現状**: `/spaces` のカード内に `note` をそのまま表示
- **問題**: `note` が長いとカードが縦に伸びて、一覧の俯瞰性が悪くなる
- **検討案**:
  - モーダル表示（カードクリックで詳細モーダル）
  - 詳細ページに切り出す（`/spaces/[id]`）
  - カードでは省略表示（`line-clamp-2` などで切る）+ ホバーで全文
- **対応時期**: Step 1-5（詳細ページ）以降で再検討
- **関連 Step**: Step 1-3

### a11y 対応（フォーム + 一覧）
- **日付**: 2026-04-24
- **現状**: 基本的なラベル関連付け（`htmlFor`/`id`）のみ
- **対応すべき項目**:
  - 必須項目の視覚マーク（`*`）と `aria-required="true"`
  - `aria-invalid` / `aria-describedby` でエラー状態の伝達
  - エラーメッセージに `role="alert"` で動的読み上げ
  - フォーカスリング（`focus:ring-2`）の明示
  - キーボード操作のみで完結することの確認
- **対応時期**: アプリ完成後、品質向上フェーズ
- **関連 Step**: Step 1-3 / 1-4

### ヘッダーのモバイル対応
- **日付**: 2026-07-08（フォーム部分のモバイル対応完了時に残した分）
- **現状**: ロゴ + ナビ3リンク + アイコン + 名前 + ログアウトで、スマホ幅だと詰まる/はみ出す恐れ
- **検討案**:
  - 名前を `hidden sm:inline` で隠す
  - ナビをハンバーガーメニュー化 or アイコン化
  - ロゴを絵文字だけにする（📦）
- **対応時期**: 品質向上フェーズ
- **関連 Step**: Step 3-3（ヘッダー）

### マージン方針（mt 派）の徹底
- **日付**: 2026-04-24
- **方針**: マージンは原則上方向（`mt-*`）または `space-y-*` で管理
- **未対応箇所**:
  - `FormField` のラベル `mb-1` → input 側に `mt-1` 付けて移動
  - 一覧ページの `<p className="... mb-3">` → 親側 `space-y` に変更
- **対応時期**: アプリ完成後、品質向上フェーズ
- **関連 Step**: Step 1-3 / 1-4

### UI の日本語化
- **日付**: 2026-04-24
- **現状**: フォームラベルが英語（Name, Width, Depth, Height, Note）
- **対応**: 「場所の名前」「幅」「奥行き」「高さ」「メモ」に統一
- **対応時期**: Step 1-7（レイアウト整理）
- **関連 Step**: Step 1-4

### フォーム送信中の disabled 状態
- **日付**: 2026-04-24
- **未対応**: `isSubmitting` を見て送信ボタンを disabled + テキスト変更
- **メリット**: 二重送信防止、UX 向上
- **対応時期**: Phase 2（実保存処理を入れる時）
- **関連 Step**: Step 1-4

### フォームのスタイル統一
- **日付**: 2026-04-24
- **現状**: input / textarea / button のサイズや余白がバラバラ気味
- **対応**: 共通のフォームコンポーネントクラス（または Tailwind プラグイン）で統一
- **対応時期**: アプリ完成後、品質向上フェーズ
- **関連 Step**: Step 1-4

---

## ✅ 対応済み

### トップページをランディングページ化（2026-07-08 完了）
- Figma Make デザインを元に5セクション構成で実装（コミット: da79498）
- ヒーロー / 共感 / 3ステップ / おすすめ（紫グラデ）/ 最終CTA
- 「登録不要」コピーは「Googleアカウントですぐに使えます」に修正済み
- CTA ボタンは signInAction（Google ログイン）に接続

### UI の日本語化（2026-06 完了）
- フォームラベルを「場所の名前 / 幅（mm）/ 高さ（mm）/ 奥行き（mm）/ メモ」に統一
- Submit → 「追加する」（new）/「更新する」（edit）に変更
- 詳細ページの Created At / Updated At → 作成日 / 更新日

### フォーム送信中の disabled 状態（2026-06 完了）
- new / edit 両フォームで isSubmitting による disabled + 文言切り替え実装

### モバイル最適化：フォーム部分（2026-06 完了）
- 全 input に text-base（iOS 自動ズーム防止）
- 数値入力に inputMode="decimal"（数字テンキー）
- py-4 でタップターゲット確保
- ※ ヘッダーのモバイル対応は未対応（改善案に残存）
